"""
Initialization module for the models package.

This module imports and makes available the `Doctor` and `Patient` models
from the respective modules within the `models` package.

Modules:
    - doctor: Contains the `Doctor` model.
    - patient: Contains the `Patient` model.
"""

from .doctor import Doctor
from .patient import Patient
